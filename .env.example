# Application
PORT=3000
NODE_ENV=development

# Secret — never commit the real value, store in GitHub Actions secrets
APP_SECRET=your-secret-here

# Feature flags — set to 'true' to enable, 'false' to disable without redeploying
FEATURE_NEW_UPDATES=true
